# Pinned to the versions the live image was built with (in-image pip freeze,
# 2026-08-28). Transitive pins live in constraints.txt — bump both together,
# test in-image, then rebuild. Unpinned ranges let a python:3.12-slim refresh
# silently re-resolve everything on a routine bot rebuild (pandas 3 / numpy 2
# are major versions; ib_insync is unmaintained).
ib_insync==0.9.86
pandas==3.0.5
numpy==2.5.2
python-telegram-bot==22.8
python-dotenv==1.2.2
tzdata==2026.3
yfinance==1.6.0
